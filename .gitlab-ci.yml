include:
  - project: 'mindtickle/devops/devops-ci-cd-templates'
    file: '/mirrored/ci-cd.yml'

event-pipeline-build:
  extends: .build
  variables:
    PROJECT_NAME: "event-pipeline"

event-pipeline-build-heimdall:
  extends: .build-heimdall
  variables:
    PROJECT_NAME: "event-pipeline"

event-pipeline-asset-deploy:
  extends: .asset-deploy
  variables:
    PROJECT_NAME: "event-pipeline"

event-pipeline-config-deploy:
  extends: .config-deploy
  variables:
    PROJECT_NAME: "event-pipeline"

event-pipeline-deploy-heimdall:
  extends: .artifact-deploy-heimdall
  variables:
    PROJECT_NAME: "event-pipeline"

event-pipeline-deploy:
  extends: .artifact-deploy
  variables:
    PROJECT_NAME: "event-pipeline"
